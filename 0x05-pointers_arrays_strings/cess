#include "main.h"

/**
 * _strlen - returns the length of a string
 * *s: pointer to string
 * @s: string
 * Return: Always 0 (Success)
 */

int _strlen(char *s)

{
	int i = 0, count = 0;

	while (s[i] != '\0')
	{
		i++;
		count++;
	}
		return(count);
}
