#include <stdio.h>
/**
 * main - main
 * @argc: argument count
 * @argv: command arguments
 *
 * Return: Always 0 (success)
 */
int main(int argc, char *argv[])
{
	(void)argv;
	{
		printf("%d\n", argc);
	}
return (0);
}
